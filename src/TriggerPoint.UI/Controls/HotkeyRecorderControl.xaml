<UserControl x:Class="TriggerPoint.UI.Controls.HotkeyRecorderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="300"
             Focusable="True"
             FocusVisualStyle="{x:Null}">
    <Grid>
        <Border x:Name="RecordingBorder"
                Background="{DynamicResource BgInputBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="8,4"
                MinHeight="36"
                MouseLeftButtonDown="RecordingBorder_MouseLeftButtonDown">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                            <Setter Property="Background" Value="{DynamicResource BgSecondaryBrush}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="PromptText" 
                               Text="Click to record shortcut..."
                               Foreground="{DynamicResource TextMutedBrush}"
                               FontStyle="Italic"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Visibility="Visible"/>
                    
                    <Border x:Name="KeyBadge"
                            Background="{DynamicResource AccentSubtleBrush}"
                            BorderBrush="{DynamicResource AccentBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="8,2"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                        <TextBlock x:Name="HotkeyDisplayText"
                                   Text=""
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontWeight="SemiBold"
                                   FontSize="12"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <TextBlock x:Name="ChordHintText"
                               Text="Press 2nd key for chord (or Enter to finish)"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="11"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                </StackPanel>

                <Button Grid.Column="1"
                        x:Name="ClearButton"
                        Content="✕"
                        Width="20"
                        Height="20"
                        FontSize="10"
                        Padding="0"
                        Margin="6,0,2,0"
                        VerticalAlignment="Center"
                        Style="{StaticResource SecondaryButtonStyle}"
                        ToolTip="Clear Shortcut"
                        Click="ClearButton_Click"
                        Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
