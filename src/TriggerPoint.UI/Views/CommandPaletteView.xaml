<Window x:Class="TriggerPoint.UI.Views.CommandPaletteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TriggerPoint Command Palette"
        Width="640"
        Height="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Deactivated="Window_Deactivated"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Border Background="{DynamicResource BgSecondaryBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="6" Opacity="0.5" Direction="270"/>
        </Border.Effect>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Search Input Box -->
            <Border Grid.Row="0"
                    Background="{DynamicResource BgInputBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12,10"
                    Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                               Text="🔍" 
                               FontSize="14" 
                               Margin="0,0,10,0" 
                               VerticalAlignment="Center"/>

                    <TextBox Grid.Column="1" 
                             x:Name="SearchTextBox"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             CaretBrush="{DynamicResource AccentBrush}"
                             FontSize="15"
                             FontFamily="{StaticResource AppFont}"
                             VerticalAlignment="Center"
                             TextChanged="SearchTextBox_TextChanged"/>

                    <Border Grid.Column="2" 
                            x:Name="ScopeBadge"
                            Style="{StaticResource KeyBadgeStyle}"
                            Visibility="Collapsed">
                        <TextBlock x:Name="ScopeText" 
                                   Text="Scoped" 
                                   FontSize="11" 
                                   Foreground="{DynamicResource AccentBrush}"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Results List -->
            <ListBox Grid.Row="1"
                     x:Name="ResultsListBox"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     SelectionMode="Single"
                     MouseDoubleClick="ResultsListBox_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="itemBorder"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="10,8"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource ItemHoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource ItemSelectedBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Type Icon / Indicator -->
                            <TextBlock Grid.Column="0"
                                       Text="{Binding IconSymbol}"
                                       FontSize="14"
                                       Width="24"
                                       VerticalAlignment="Center"
                                       Foreground="{Binding IconBrush}"/>

                            <!-- Match Highlight Title & Description / Detail -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="6,0,12,0">
                                <TextBlock Text="{Binding Name}"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding SecondaryDetail}"
                                           FontSize="11"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis"
                                           Visibility="{Binding HasSecondaryDetail, Converter={x:Null}}"/>
                            </StackPanel>

                            <!-- Badges: Hotkey + Usage Count -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Style="{StaticResource AccentKeyBadgeStyle}" 
                                        Margin="0,0,6,0"
                                        Visibility="{Binding HasHotkey, Converter={x:Null}}">
                                    <TextBlock Text="{Binding HotkeyText}" 
                                               FontSize="11" 
                                               Foreground="{DynamicResource AccentBrush}"/>
                                </Border>
                                <TextBlock Text="{Binding LaunchCountBadge}" 
                                           FontSize="10" 
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Modifier overrides footer -->
            <Border Grid.Row="2"
                    BorderBrush="{DynamicResource BorderSubtleBrush}"
                    BorderThickness="0,1,0,0"
                    Margin="0,10,0,0"
                    Padding="0,8,0,0">
                <TextBlock FontSize="11"
                           Foreground="{DynamicResource TextMutedBrush}"
                           HorizontalAlignment="Center">
                    <Run Text="⏎" FontSize="13.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/><Run Text=" Run   •   "/><Run Text="Ctrl+" Foreground="{DynamicResource TextSecondaryBrush}"/><Run Text="⏎" FontSize="13.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/><Run Text=" Run as Admin   •   "/><Run Text="Shift+" Foreground="{DynamicResource TextSecondaryBrush}"/><Run Text="⏎" FontSize="13.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/><Run Text=" Reveal in Explorer   •   "/><Run Text="Alt+" Foreground="{DynamicResource TextSecondaryBrush}"/><Run Text="⏎" FontSize="13.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/><Run Text=" Open in Settings"/>
                </TextBlock>
            </Border>
        </Grid>
    </Border>
</Window>
