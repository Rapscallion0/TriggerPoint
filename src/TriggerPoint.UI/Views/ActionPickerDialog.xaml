<Window x:Class="TriggerPoint.UI.Views.ActionPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Action or Folder"
        Width="560" Height="520"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        KeyDown="Window_KeyDown">

    <Border Background="{DynamicResource BgSecondaryBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.4" Direction="270"/>
        </Border.Effect>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Select Action or Folder" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="Choose an action to execute or folder to open in this workflow step" 
                               FontSize="12" 
                               Foreground="{DynamicResource TextSecondaryBrush}" 
                               Margin="0,2,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" 
                        Content="✕" 
                        Style="{StaticResource WindowControlButtonStyle}" 
                        Width="28" Height="28" 
                        Click="CancelBtn_Click"/>
            </Grid>

            <!-- Search Filter Box -->
            <Border Grid.Row="1" Margin="0,0,0,10">
                <TextBox x:Name="SearchBox"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Height="34"
                         Tag="Search actions and folders by name or type..."
                         ToolTip="Filter by action name or type"
                         TextChanged="SearchBox_TextChanged"/>
            </Border>

            <!-- Items List -->
            <Border Grid.Row="2" 
                    Background="{DynamicResource BgPrimaryBrush}" 
                    BorderBrush="{DynamicResource BorderBrush}" 
                    BorderThickness="1" 
                    CornerRadius="8"
                    Margin="0,0,0,14">
                <ListBox x:Name="ItemsListBox"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="4"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         MouseDoubleClick="ItemsListBox_MouseDoubleClick"
                         SelectionChanged="ItemsListBox_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="6" Padding="8,6" Margin="0,1" Background="Transparent">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon -->
                                    <TextBlock Grid.Column="0" 
                                               Text="{Binding IconSymbol}" 
                                               FontSize="15" 
                                               Margin="0,0,10,0" 
                                               VerticalAlignment="Center"/>

                                    <!-- Name & Breadcrumb Path -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" 
                                                   FontSize="13" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding PathDisplay}" 
                                                   FontSize="11" 
                                                   Foreground="{DynamicResource TextSecondaryBrush}" 
                                                   TextTrimming="CharacterEllipsis"
                                                   Margin="0,1,0,0"/>
                                    </StackPanel>

                                    <!-- Type / Action Badge -->
                                    <Border Grid.Column="2" 
                                            Background="{DynamicResource AccentSubtleBrush}"
                                            BorderBrush="{DynamicResource BorderSubtleBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="6,2"
                                            VerticalAlignment="Center"
                                            Margin="8,0,0,0">
                                        <TextBlock Text="{Binding TypeBadgeText}" 
                                                   FontSize="10.5" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource AccentBrush}"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Bottom Bar: Status and Actions -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           x:Name="StatusCountText" 
                           Text="0 items" 
                           FontSize="12" 
                           Foreground="{DynamicResource TextSecondaryBrush}" 
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Cancel" 
                            Style="{StaticResource SecondaryButtonStyle}" 
                            Padding="14,6" 
                            Margin="0,0,8,0"
                            Click="CancelBtn_Click"/>
                    <Button x:Name="SelectBtn" 
                            Content="Select" 
                            Style="{StaticResource AccentButtonStyle}" 
                            Padding="16,6"
                            IsEnabled="False"
                            Click="SelectBtn_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
