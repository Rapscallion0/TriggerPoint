<Window x:Class="TriggerPoint.UI.Views.ChordHudView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TriggerPoint Chord Indicator"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Focusable="False"
        SizeToContent="WidthAndHeight">
    <Border Background="{DynamicResource BgPrimaryBrush}"
            BorderBrush="{DynamicResource AccentBrush}"
            BorderThickness="1.5"
            CornerRadius="10"
            Padding="14,10"
            Margin="12"
            MinWidth="380"
            MaxWidth="600">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="22" ShadowDepth="4" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="⚡" FontSize="14" Foreground="{DynamicResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="LeaderText" Text="Ctrl + K" FontWeight="Bold" FontSize="13" Foreground="{DynamicResource AccentBrush}"/>
                    <TextBlock Text=", ... waiting for second key" FontSize="12.5" Foreground="{DynamicResource TextPrimaryBrush}" Margin="4,0,0,0"/>
                </StackPanel>
                <TextBlock Grid.Column="2" Text="Esc cancel" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center" Margin="14,0,0,0"/>
            </Grid>

            <!-- Available chord choices list -->
            <ItemsControl x:Name="ChordOptionsItemsControl" Margin="0,8,0,0" Visibility="Collapsed">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource BgSecondaryBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="6,2"
                                Margin="0,0,6,4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding KeyDisplay}" FontWeight="Bold" FontSize="11" Foreground="{DynamicResource AccentBrush}"/>
                                <TextBlock Text="{Binding ActionName}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="4,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>
